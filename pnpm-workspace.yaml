allowBuilds:
  esbuild: true
  sharp: true

# @astrojs/react@6 pulls a second vite (8.1.3) which both breaks types (dual
# vite) and pins postcss@^8.5.16 (unpublished). Force a single vite matching
# Astro's, and pin postcss to the latest real release.
overrides:
  vite: 7.3.2
  postcss: 8.5.15
