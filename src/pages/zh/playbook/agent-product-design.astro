---
import { getRelativeLocaleUrl } from "astro:i18n";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Breadcrumb from "@/components/Breadcrumb.astro";
import Main from "@/components/Main.astro";
import Footer from "@/components/Footer.astro";
import config from "@/config";

const locale = "zh";
---

<Layout
  title={`Agent Product Design — ${config.site.title}`}
  description="如何设计 AI 智能体产品：模型推理、工具、记忆、工作流、权限、治理，以及 Human-in-the-Loop 模式。"
>
  <Header />
  <Breadcrumb />

  <Main pageTitle="智能体产品设计（Agent Product Design）" class="app-prose">
    <p class="text-muted-foreground text-sm">
      <a
        href={getRelativeLocaleUrl("en", "playbook/agent-product-design")}
        class="text-accent hover:underline"
      >
        Read this page in English
      </a>
    </p>

    <p class="text-muted-foreground text-lg">
      智能体产品不只是聊天机器人。它们把模型推理、工具、记忆、工作流、权限与治理组合在一起。
    </p>

    <h2 class="mt-8 text-xl font-semibold">Agent 不等于 Chat</h2>
    <p>
      「智能体（Agent）」这个词经常被随意使用。一个回答问题的聊天机器人并不是智能体。智能体会感知上下文、围绕目标进行推理、选择并使用工具、采取行动，并处理失败。产品设计的挑战，在于把所有这些组件编排成一种可靠的用户体验。
    </p>

    <h2 class="mt-6 text-xl font-semibold">
      智能体 = 模型 + 工具 + 记忆 + 工作流 + 治理
    </h2>
    <ul class="mt-2 space-y-3">
      <li>
        <strong>模型（Model）</strong> —— 推理引擎。用哪个模型、什么 temperature、多大的上下文窗口？模型的选择会影响可靠性、延迟和成本。
      </li>
      <li>
        <strong>工具（Tools）</strong> —— 智能体能读什么、写什么？API、数据库、文件系统、外部服务。每一个工具既是一种能力，也是一种风险。
      </li>
      <li>
        <strong>记忆（Memory）</strong> —— 短期记忆（会话上下文）和长期记忆（学到的偏好、知识库）。记忆决定了智能体是越来越聪明，还是转头就忘。
      </li>
      <li>
        <strong>工作流（Workflow）</strong> —— 编排逻辑。多步推理、规划、执行顺序、错误恢复，以及重试策略。
      </li>
      <li>
        <strong>治理（Governance）</strong> —— 权限、审计日志、人工审批关卡、限流，以及合规规则。没有治理，智能体就是一种负债。
      </li>
    </ul>

    <h2 class="mt-6 text-xl font-semibold">设计要回答的问题</h2>
    <ul class="mt-2 space-y-2">
      <li>智能体能读取什么？</li>
      <li>智能体能做什么？</li>
      <li>它在什么时候需要请求审批？</li>
      <li>它如何从失败中恢复？</li>
      <li>哪些内容会被记录和审计？</li>
      <li>如果智能体做出错误决策，影响半径（blast radius）有多大？</li>
      <li>用户如何纠正或覆盖智能体的行为？</li>
    </ul>

    <h2 class="mt-6 text-xl font-semibold">常见错误</h2>
    <ul class="mt-2 space-y-2">
      <li>
        给智能体的范围过大 —— 在证明可靠性之前，就给了它太多工具和太高的自主性。
      </li>
      <li>
        没有清晰的失败恢复机制 ——
        如果智能体在任务中途失败，用户会看到什么？系统如何回到一个干净的状态？
      </li>
      <li>
        把 HITL 当作事后补丁 —— Human-in-the-Loop
        是核心产品流程，而不是事后才拴上去的安全网。
      </li>
      <li>
        忽视治理 —— 在企业场景里，审计轨迹、权限边界和审批流程都不是可选项。
      </li>
    </ul>

    <h2 class="mt-6 text-xl font-semibold">我的处理方式</h2>
    <p>
      我从本体（ontology）出发：存在哪些实体、哪些关系重要、可能有哪些动作。然后我定义工具权限和
      HITL 检查点。模型和工作流放在最后 ——
      一旦产品结构清晰了，它们就只是实现细节。
    </p>

    <div class="border-border mt-8 rounded-lg border p-6">
      <h2 class="text-lg font-semibold">相关</h2>
      <div class="mt-3 flex flex-wrap gap-3">
        <a
          href={getRelativeLocaleUrl(locale, "playbook/prompt-to-ontology")}
          class="text-accent hover:underline"
        >
          Prompt-to-Ontology
        </a>
        <a
          href={getRelativeLocaleUrl(locale, "playbook/ai-native-prd")}
          class="text-accent hover:underline"
        >
          AI-Native PRD
        </a>
        <a
          href={getRelativeLocaleUrl(locale, "projects/prompt-to-ontology")}
          class="text-accent hover:underline"
        >
          Prompt-to-Ontology 项目
        </a>
        <a
          href={getRelativeLocaleUrl(locale, "playbook")}
          class="text-accent hover:underline"
        >
          Playbook 索引
        </a>
      </div>
    </div>
  </Main>

  <Footer />
</Layout>
